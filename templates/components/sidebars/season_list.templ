package sidebars

import "github.com/JulOuellet/blurred-app/internal/domains/seasons"

templ SeasonsList(seasonList []seasons.SeasonModel, activeID string) {
	for _, season := range seasonList {
		<li>
			@SeasonItem(season, season.ID.String() == activeID)
		</li>
	}
}
